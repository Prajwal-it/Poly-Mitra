# Pinned versions — match exactly what the model was trained with.
# NEVER upgrade catboost, scikit-learn, or joblib without retraining and re-saving the model,
# because newer versions can refuse to load a .pkl saved by an older version.

pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
catboost==1.2.10
joblib==1.5.3
flask==3.1.3
flask-cors==6.0.5
python-dotenv==1.1.1
gunicorn==23.0.0